# 用户助手系统集成指南

## 概述

用户助手系统是一个基于Spring Boot的Starter组件，提供了完整的前端界面和后端API，支持手册管理、技术支持和用户反馈功能。本文档详细说明如何在您的项目中集成此系统。

## 功能特性

- ✅ **手册管理** - 创建、编辑、查看技术手册
- ✅ **技术支持** - 管理技术支持文档
- ✅ **用户反馈** - 收集和管理用户反馈
- ✅ **文件上传** - 支持图片上传功能
- ✅ **前端界面** - Vue 3 + Element Plus 现代化界面
- ✅ **API文档** - 集成Swagger文档
- ✅ **一键启用** - 通过注解快速启用

## 快速开始

### 1. 添加Maven依赖

在您的Spring Boot项目的 `pom.xml` 文件中添加以下依赖：

```xml
<dependency>
    <groupId>com.module</groupId>
    <artifactId>user-assistant-spring-boot-starter</artifactId>
    <version>1.0.0</version>
</dependency>
```

### 2. 启用用户助手功能

在您的Spring Boot主启动类上添加 `@EnableUserAssistant` 注解：

```java
package com.yourcompany.yourproject;

import com.module.starter.annotation.EnableUserAssistant;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
@EnableUserAssistant  // 启用用户助手功能
public class YourApplication {
    
    public static void main(String[] args) {
        SpringApplication.run(YourApplication.class, args);
    }
}
```

### 3. 配置数据库

在 `application.yml` 中配置数据库连接：

```yaml
# 数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/your_database?serverTimezone=GMT%2b8&useUnicode=true&characterEncoding=UTF8&useSSL=false&allowMultiQueries=true
    username: your_username
    password: your_password

# MyBatis Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
```

### 4. 创建数据库表

执行以下SQL脚本创建必要的数据库表：

```sql
-- 手册文章表
CREATE TABLE `ass_manual_articles` (
    `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `title` VARCHAR(255) NOT NULL COMMENT '标题',
    `content` LONGTEXT COMMENT '内容',
    `author` VARCHAR(100) COMMENT '作者',
    `sort_order` INT(11) DEFAULT 0 COMMENT '排序',
    `created_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除',
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='手册文章表';

-- 技术支持文章表
CREATE TABLE `ass_support_articles` (
    `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `title` VARCHAR(255) NOT NULL COMMENT '标题',
    `content` LONGTEXT COMMENT '内容',
    `author` VARCHAR(100) COMMENT '作者',
    `sort_order` INT(11) DEFAULT 0 COMMENT '排序',
    `created_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除',
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='技术支持文章表';

-- 用户反馈表
CREATE TABLE `ass_feedbacks` (
    `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `content` LONGTEXT NOT NULL COMMENT '反馈内容',
    `contact_info` VARCHAR(255) COMMENT '联系方式',
    `status` INT(11) DEFAULT 0 COMMENT '状态：0-待处理，1-处理中，2-已解决',
    `created_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted` TINYINT(1) DEFAULT 0 COMMENT '逻辑删除',
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户反馈表';

-- 反馈图片表
CREATE TABLE `ass_feedback_images` (
    `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `feedback_id` BIGINT(20) NOT NULL COMMENT '反馈ID',
    `image_url` VARCHAR(500) NOT NULL COMMENT '图片URL',
    `created_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    KEY `idx_feedback_id` (`feedback_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='反馈图片表';
```

## 详细配置

### 完整配置示例

```yaml
server:
  port: 8080

# 用户助手配置
user-assistant:
  enabled: true  # 启用用户助手功能
  api-prefix: /api  # API路径前缀
  table-prefix: ass_  # 数据库表前缀
  enable-logging: true  # 启用日志记录
  
  # 文件上传配置
  upload:
    path: ${user.dir}/uploads  # 上传目录
    url-prefix: /uploads  # URL前缀
    allowed-types: image/jpeg,image/png,image/gif  # 允许的文件类型
    max-size: 2MB  # 最大文件大小
    max-request-size: 10MB  # 最大请求大小
  
  # Swagger配置
  swagger:
    enabled: true  # 启用Swagger文档
    title: 用户助手API  # 文档标题
    description: 用户助手系统接口文档  # 文档描述
    version: 1.0.0  # API版本
    base-package: com  # 扫描包路径

# 数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/your_database?serverTimezone=GMT%2b8&useUnicode=true&characterEncoding=UTF8&useSSL=false&allowMultiQueries=true
    username: your_username
    password: your_password

# MyBatis Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# 日志配置
logging:
  level:
    root: INFO
    com.module: DEBUG
```

### 配置参数说明

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `user-assistant.enabled` | `true` | 是否启用用户助手功能 |
| `user-assistant.api-prefix` | `/api` | API路径前缀 |
| `user-assistant.table-prefix` | `ass_` | 数据库表名前缀 |
| `user-assistant.enable-logging` | `true` | 是否启用详细日志 |
| `user-assistant.upload.path` | `${user.dir}/uploads` | 文件上传存储路径 |
| `user-assistant.upload.url-prefix` | `/uploads` | 上传文件访问URL前缀 |
| `user-assistant.upload.allowed-types` | `image/jpeg,image/png,image/gif` | 允许上传的文件类型 |
| `user-assistant.upload.max-size` | `2MB` | 单个文件最大大小 |
| `user-assistant.upload.max-request-size` | `10MB` | 单次请求最大大小 |
| `user-assistant.swagger.enabled` | `true` | 是否启用Swagger文档 |
| `user-assistant.swagger.title` | `用户助手API` | Swagger文档标题 |
| `user-assistant.swagger.description` | `用户助手系统接口文档` | Swagger文档描述 |
| `user-assistant.swagger.version` | `1.0.0` | API版本号 |
| `user-assistant.swagger.base-package` | `com` | Swagger扫描的包路径 |

## 访问地址

启动应用后，可以通过以下地址访问：

- **主页界面**: `http://localhost:8080/`
- **API文档**: `http://localhost:8080/swagger-ui.html`
- **手册管理**: `http://localhost:8080/manual`
- **技术支持**: `http://localhost:8080/support` 
- **用户反馈**: `http://localhost:8080/feedback`

## API接口

### 手册管理API

- `GET /api/manual/list` - 获取所有手册
- `POST /api/manual/page` - 分页查询手册
- `GET /api/manual/{id}` - 获取指定手册
- `POST /api/manual/add` - 新增手册
- `POST /api/manual/update` - 更新手册
- `DELETE /api/manual/{id}` - 删除手册

### 技术支持API

- `GET /api/support/list` - 获取所有技术支持文档
- `POST /api/support/page` - 分页查询技术支持文档
- `GET /api/support/{id}` - 获取指定技术支持文档
- `POST /api/support/add` - 新增技术支持文档
- `POST /api/support/update` - 更新技术支持文档
- `DELETE /api/support/{id}` - 删除技术支持文档

### 用户反馈API

- `GET /api/feedback/list` - 获取所有反馈
- `POST /api/feedback/page` - 分页查询反馈
- `GET /api/feedback/{id}` - 获取指定反馈
- `POST /api/feedback/add` - 新增反馈
- `POST /api/feedback/update` - 更新反馈状态
- `DELETE /api/feedback/{id}` - 删除反馈

### 文件上传API

- `POST /api/upload/image` - 上传图片

## 高级集成

### 自定义配置

如果需要在您的项目中自定义某些行为，可以通过以下方式：

#### 1. 禁用某些功能

```yaml
user-assistant:
  swagger:
    enabled: false  # 禁用Swagger
```

#### 2. 自定义上传路径

```yaml
user-assistant:
  upload:
    path: /opt/uploads  # 自定义上传路径
    url-prefix: /files  # 自定义URL前缀
```

#### 3. 修改API前缀

```yaml
user-assistant:
  api-prefix: /assistant-api  # 修改API前缀
```

### 集成到现有项目

如果您的项目已经有认证、权限等功能，可以通过以下方式集成：

#### 1. 添加权限控制

创建一个拦截器或过滤器来控制访问权限：

```java
@Component
public class UserAssistantSecurityInterceptor implements HandlerInterceptor {
    
    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
        String requestURI = request.getRequestURI();
        
        // 检查是否是用户助手相关的请求
        if (requestURI.startsWith("/api/manual") || 
            requestURI.startsWith("/api/support") || 
            requestURI.startsWith("/api/feedback")) {
            
            // 在这里添加您的权限检查逻辑
            // 例如：检查用户是否已登录，是否有相应权限等
            return checkUserPermission(request);
        }
        
        return true;
    }
    
    private boolean checkUserPermission(HttpServletRequest request) {
        // 实现您的权限检查逻辑
        return true;
    }
}
```

#### 2. 注册拦截器

```java
@Configuration
public class WebMvcConfig implements WebMvcConfigurer {
    
    @Autowired
    private UserAssistantSecurityInterceptor securityInterceptor;
    
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(securityInterceptor)
                .addPathPatterns("/api/**");
    }
}
```

## 故障排除

### 常见问题

#### 1. 404错误

**问题**: 访问 `http://localhost:8080/api/manual/list` 返回404
**解决方案**: 
- 确保已添加 `@EnableUserAssistant` 注解
- 检查数据库配置是否正确
- 查看启动日志是否有错误信息

#### 2. 数据库连接失败

**问题**: 启动时报数据库连接错误
**解决方案**:
- 检查数据库连接配置
- 确保数据库服务正在运行
- 验证用户名密码是否正确

#### 3. 文件上传失败

**问题**: 上传文件时报错
**解决方案**:
- 检查上传目录是否存在且有写入权限
- 确认文件大小和类型是否符合配置要求
- 查看 `user-assistant.upload.path` 配置是否正确

#### 4. Swagger无法访问

**问题**: 访问 `http://localhost:8080/swagger-ui.html` 返回404
**解决方案**:
- 确保 `user-assistant.swagger.enabled` 为 `true`
- 检查项目中是否缺少Swagger相关依赖
- 查看控制台是否有Swagger相关错误日志

### 调试模式

启用调试模式以获取更详细的日志：

```yaml
logging:
  level:
    root: INFO
    com.module: DEBUG
    org.springframework.web: DEBUG
```

## 版本更新

### 更新Starter版本

当有新版本发布时，更新 `pom.xml` 中的版本号：

```xml
<dependency>
    <groupId>com.module</groupId>
    <artifactId>user-assistant-spring-boot-starter</artifactId>
    <version>1.1.0</version> <!-- 更新版本号 -->
</dependency>
```

### 数据库迁移

如果新版本涉及数据库结构变更，请查看相应的迁移脚本并执行。

## 技术支持

如果您在集成过程中遇到问题，请：

1. 查看本文档的故障排除部分
2. 检查GitHub仓库的Issues页面
3. 联系技术支持团队

## 许可证

本项目采用 MIT 许可证，详见 LICENSE 文件。

---

**最后更新**: 2025-06-20  
**版本**: 1.0.0 