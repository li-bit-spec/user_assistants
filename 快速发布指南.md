# 🚀 Maven中央仓库发布快速指南

## 📍 当前状态
✅ 项目构建成功 (`mvn install`)
✅ GroupId已改为 `io.github.li-bit-spec`
❌ 缺少Sonatype认证配置

## 🎯 完成发布的步骤

### 1. 注册Sonatype账户
访问 [issues.sonatype.org](https://issues.sonatype.org) 注册JIRA账户

### 2. 申请命名空间权限
创建一个JIRA issue，申请 `io.github.li-bit-spec` 权限：
- **Project**: Community Support - Open Source Project Repository Hosting (OSSRH)
- **Issue Type**: New Project
- **Summary**: Request for io.github.li-bit-spec
- **Group Id**: io.github.li-bit-spec
- **Project URL**: https://github.com/li-bit-spec/user_assistants
- **SCM URL**: https://github.com/li-bit-spec/user_assistants.git

### 3. 验证GitHub所有权
在你的GitHub账户中创建一个公开仓库：`OSSRH-XXXXX`（XXXXX是JIRA issue号）

### 4. 配置GPG密钥
```bash
# 生成密钥（如果没有）
gpg --gen-key

# 查看密钥ID
gpg --list-secret-keys --keyid-format LONG

# 发布公钥
gpg --keyserver keyserver.ubuntu.com --send-keys YOUR_KEY_ID
```

### 5. 更新settings.xml
编辑 `C:\Users\李华宪\.m2\settings.xml`，替换：
```xml
<server>
    <id>central</id>
    <username>your-jira-username</username>    <!-- 你的JIRA用户名 -->
    <password>your-jira-password</password>    <!-- 你的JIRA密码 -->
</server>
```

和GPG配置：
```xml
<gpg.keyname>YOUR_ACTUAL_KEY_ID</gpg.keyname>
<gpg.passphrase>your-gpg-passphrase</gpg.passphrase>
```

### 6. 发布
```bash
cd user-assistant-spring-boot-starter
mvn clean deploy -P release
```

## ⏱️ 预计时间
- 申请权限：1-2个工作日
- 配置和发布：30分钟

## 🎯 快速验证当前可用性

即使不发布到中央仓库，你的Starter现在就可以使用：

```bash
# 在其他项目中
mvn clean install  # 会从本地仓库找到你的Starter
```

## 📋 建议

对于企业内部使用或快速原型，本地安装就足够了。
只有需要公开分享给其他开发者时，才需要发布到Maven中央仓库。 